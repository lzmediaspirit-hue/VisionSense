import { ScreenHeader } from "../components/ui";
import { strings } from "../copy/strings";

// M0 placeholder. Desired Reality detail + habits lands in M1.
export function GoalDetailScreen() {
  return (
    <div>
      <ScreenHeader title={strings.goals.title} />
    </div>
  );
}
